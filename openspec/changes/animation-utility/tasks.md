# Tasks — Animation Utility

## Milestone 1: Create Utility

- [ ] **TASK-01** Create `src/lib/animations.ts` with `staggerContainer`, `fadeUpItem`, `scaleOnHover`

## Milestone 2: Migrate Components

- [ ] **TASK-02** Update `src/components/home/HeroBanner.tsx` — replace inline variants with imports
- [ ] **TASK-03** Update `src/components/home/FeaturedProducts.tsx` — replace inline variants with imports
- [ ] **TASK-04** Update `src/components/home/CategoryCards.tsx` — replace inline variants with imports

## Verification

- [ ] HeroBanner stagger animation unchanged on home page load
- [ ] FeaturedProducts card stagger unchanged on scroll
- [ ] CategoryCards useInView stagger unchanged
- [ ] No TypeScript errors (`Variants` type from framer-motion)
