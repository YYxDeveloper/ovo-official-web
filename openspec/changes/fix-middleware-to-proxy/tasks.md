# Tasks

## Completed

- [x] Delete `src/middleware.ts`
- [x] Create `src/proxy.ts` with `export function proxy()` and identical auth guard logic
- [x] Verify build passes

## TODO

- [ ] E2E test: unauthenticated `/admin` → redirect to `/admin/login`
- [ ] E2E test: authenticated `/admin` → renders dashboard
